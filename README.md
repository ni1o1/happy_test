# React + Vite + Ant Design 测试项目

这是一个使用 React 18、Vite 和 Ant Design 5 构建的简单测试项目。

## 功能特点

- ⚡ 使用 Vite 作为构建工具，提供快速的开发体验
- 🔥 React 18 支持
- 🎨 Ant Design 5 组件库
- 📦 构建输出到 `docs` 文件夹，便于 GitHub Pages 部署

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

### 构建项目

```bash
npm run build
```

构建产物将输出到 `docs` 文件夹。

## 项目结构

```
react-antd-test/
├── docs/              # 构建输出目录
├── src/               # 源代码
│   ├── App.jsx        # 主应用组件
│   └── main.jsx       # 应用入口
├── index.html         # HTML 入口文件
├── package.json       # 项目配置
└── vite.config.js     # Vite 配置
```

## GitHub Pages 部署

由于构建输出到 `docs` 文件夹，你可以在 GitHub 仓库设置中启用 GitHub Pages，并选择从 `docs` 文件夹部署。

## 许可证

MIT
